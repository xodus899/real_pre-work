var countries;

countries = [];

countries.push ("Russia","Finland")
countries.push ("Canada");

console.log(countries);
// This is to add together .push

var countries, i;

countries = [ "Russia", "Finland", "Morocco", "Brazil" ];

for (i = 0; i < countries.length; i = i + 1) {
    console.log(countries[i]);
}

// This is to start at the count of 0 ... and add up to 3 
// >length knows how many are in the array...
// arrays start at the number 0

